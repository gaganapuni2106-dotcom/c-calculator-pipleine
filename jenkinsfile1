pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                sh 'make calculator'
            }
        }

        stage('Test') {
            steps {
                sh 'make test'
            }
        }
    }

    post {
        always {
            echo 'Pipeline completed successfully!'
        }
    }
}
